#include <stdio.h>
int main ();
{
int F;
float C;
printf("Enter the temperature in degrees fahrenheit:\n\n");
scanf("%i", &f);
C = (5*9) / (F-32);
printf ("The converted temperature is %f \n", celsius);
return 0;
}
